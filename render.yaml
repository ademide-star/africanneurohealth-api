services:
  - type: web
    name: africanneurohealth-api
    env: python
    pythonVersion: "3.11.9"
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
